
<nav>
    <ul class="pagination justify-content-center mt-4">
        @foreach (var link in _links)
        {
            <li @key="link" @onclick="() => OnSelectedPage(link)" style="cursor: pointer;" class="page-item @(link.Enabled ? null : "disabled") @(link.Active ? "active" : null)">
                <span class="page-link" href="#">@link.Text</span>
            </li>
        }
    </ul>
</nav>




